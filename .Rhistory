#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(table(base_rasgos$family), decreasing = TRUE)
#The first thing we need to do is loading the database.
#Probably is always better to set the working directory in the scripts folders.
setwd("scripts/")
#Then we need to load the database
base_rasgos <- read.csv("../data/Base_Clase_Rasgos.csv")
#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(table(base_rasgos$family), decreasing = TRUE)
?sort
#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(base_rasgos$family, decreasing = T)
#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(table(base_rasgos$family, decreasing = T))
#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(table(base_rasgos$family, decreasing = T)
#Can we sort de table from families with more species to families with less?
#Hacer tabla en orden decreciente de familias con más especies a familias con menos especies.
sort(table(base_rasgos$family, decreasing = T))
sort(table(base_rasgos$family), decreasing = T)
sort(table(base_rasgos$family), decreasing = F)
sort(table(base_rasgos$family), decreasing = T)
#cargar todas las librerias que se van a usar
library(dplyr)
library(car)
library(ggplot2)
#traits<- read.csv("2020_10_29_Base.csv") #cargar base de datos#
traits<- read.csv("data/2020_11_12_Base.csv") #cargar base de datos#
#traits<- read.csv("2020_10_29_Base.csv") #cargar base de datos#
traits<- read.csv("8-10-2020.csv") #cargar base de datos#
#Cargando la base de datos
traits <- read.csv("data/2020_11_12_Base.csv")
#Cargando la base de datos
traits <- read.csv("2020_12_02_Base_curso_rasgos.csv")
#Cargando la base de datos
traits <- read.csv("data/2020_12_02_Base_curso_rasgos.csv")
#Cargando la base de datos
traits <- read.csv("data/2020-12-02_Base_curso_rasgos.csv")
View(traits)
names(traits) #revisar nombres#
str(traits)
q()
